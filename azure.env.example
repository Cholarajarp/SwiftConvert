# Azure environment variables example (DO NOT add real secrets to version control)
# Set these values in Azure App Service (Application Settings) or use Key Vault and Managed Identity

# Storage (Blob) connection string
AZURE_STORAGE_CONNECTION_STRING=

# Database connection (Postgres or MySQL)
DATABASE_URL=postgresql://<user>:<password>@<hostname>:5432/<db_name>

# Key Vault (optional)
KEY_VAULT_URI=https://<your-keyvault-name>.vault.azure.net/

# Stripe (do NOT commit live keys)
STRIPE_PUBLIC_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# (Optional) Web App name - used by GitHub Action if set as a secret
AZURE_WEBAPP_NAME=

# Note: Deployment tokens and publish profiles are stored as GitHub repository secrets:
# - AZURE_STATIC_WEB_APPS_API_TOKEN
# - AZURE_WEBAPP_PUBLISH_PROFILE
# - AZURE_WEBAPP_NAME (optional)
